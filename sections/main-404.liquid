<div class="page-width error-page">
  <h1>{{ '404.title' | t }}</h1>
  <p>{{ '404.subtext' | t }}</p>
  <a href="{{ routes.root_url }}" class="btn btn--primary">{{ '404.link' | t }}</a>
</div>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
